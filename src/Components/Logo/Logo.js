import n from '../../static/img/n.svg';
const Logo = () => {
  return (
    <div className='logo-cntnr'>
      <p>{`{`}</p>
      <img src={n} alt='' />
      <p>{`}`}</p>
    </div>
  );
};

export default Logo;
